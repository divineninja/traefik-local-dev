tls:
  certificates:
    - certFile: /etc/ssl/traefik/local.test.crt
      keyFile: /etc/ssl/traefik/local.test.key
  stores:
    default:
      defaultCertificate:
        certFile: /etc/ssl/traefik/local.test.crt
        keyFile: /etc/ssl/traefik/local.test.key
